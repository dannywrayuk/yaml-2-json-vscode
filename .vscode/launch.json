{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/yaml-2-json-vscode"
      ],
      "outFiles": ["${workspaceFolder}/yaml-2-json-vscode/dist/**/*.js"],
      "preLaunchTask": "${defaultBuildTask}"
    }
  ]
}
